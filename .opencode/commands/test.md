---
description: Run tests with coverage
agent: build
model: opencode/haiku
---

Run the full test suite with coverage report. Focus on any failing tests and suggest fixes.

Steps:
1. Run `npm test` or the project's test command
2. Show coverage summary
3. Identify failing tests
4. Suggest fixes for failures
